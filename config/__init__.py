# config/__init__.py
"""
Configuration package: constants, settings, styles, and path utilities.
Import specific submodules where needed, e.g.:

    from config.paths import resource_path, base_dir
    from config.styles import SCROLLBAR_STYLE
"""
